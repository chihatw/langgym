import MngChecklistForm from '@/features/checklist/components/MngChecklistForm';

type Props = {};

const ChecklistPage = (props: Props) => {
  return <MngChecklistForm />;
};

export default ChecklistPage;
